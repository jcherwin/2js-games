export function checkWinner(board) {
    // Implement logic to check for a win (horizontal, vertical, or diagonal)
    // Return 'X', 'O', or null (no winner yet)
}

export function isBoardFull(board) {
    // Implement logic to check if the board is full (no empty cells left)
    // Return true if the board is full, false otherwise
}

